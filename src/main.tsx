import React from 'react';
import ReactDOM from 'react-dom/client';
import ChallengeApp from './ChallengeApp.tsx';

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <ChallengeApp />
    </React.StrictMode>
);
